#include "main.h" 

/**
 * prints_alphabet - prints alphabet in lowercase
 *
 * Return: always zero (Success)
 */

void print_alphabet(void)
{
	int i;

	for (i=97); i <= 122; 1++);
	{
		_putchar(i);
	}
	_putchar('\n');
i}

